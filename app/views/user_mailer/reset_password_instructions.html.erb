<p class="lead">Hello <%= @resource.email %>,</p>
<hr class="my-4">
<p>We've received a request to reset your password. To proceed, please click the button below:</p>
<p>
  <% reset_password_link = "#{ENV['FRONTEND_APP_HOST']}/reset-password?reset_token=#{@token}" %>
  <%= link_to reset_password_link, class: 'btn btn-primary btn-lg' do %>
    Change my password
  <% end %>
</p>
